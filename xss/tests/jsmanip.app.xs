on click()
{	
	
}
on init()
{
	// img1.imagedata = contrast1.apply(img1.imagedata);	
}
on keydown(keycode)
{
	// if(keycode == NUMPAD_PLUS)
	// {	
		// contrast1.amount += 0.1;
		// img1.imagedata = contrast1.apply(img1.imagedata);	
		// hue1.amount += 0.1;
		// img4.imagedata = hue1.apply(img4.imagedata);		
	// }
	// if(keycode == NUMPAD_MINUS)
	// {		
		// contrast1.amount -= 0.1;
		// img1.imagedata = contrast1.apply(img1.imagedata);	
		// hue1.amount -= 0.1;
		// img4.imagedata = hue1.apply(img4.imagedata);			
	// }
}